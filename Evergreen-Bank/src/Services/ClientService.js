/*import { ref } from "vue"

export default class ClientService {
    clienteslista
    constructor(){
        this.clienteslista = ref ([])
    }
    getClientesLista() {
        return this.clienteslista
    }

    async getclientes() {
        const response = await fetch(`http://${import.meta.env.VITE_API}/`)
    }
}*/